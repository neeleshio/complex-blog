import React from 'react';

const SectionHeader = () => {
    return (
        <div className="py-[1.2rem] bg-blur sticky top-0 w-full bg-white bg-blur">
            <div className="max-w-[98rem] m-auto flex items-center">
                <h1 className="text-[2rem] font-semibold">Blog Wall</h1>
                <div></div>
            </div>
        </div>
    );
};

export default SectionHeader;
